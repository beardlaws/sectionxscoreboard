# Immediate next step

Let Vercel build `feature/game-center-v1`. If preview is Ready, test generic and game-deep-linked photo submission. Then merge the safe photo/foundation release before starting the separate public Game Center integration/admin editor pass.
