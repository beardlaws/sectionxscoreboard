# Period scoring

`game_period_scores` uses a numeric order plus optional label so the same model can represent Q1-Q4, hockey periods, innings, volleyball sets, overtime, or other ordered scoring segments.

Sport-specific admin UI should choose friendly default labels; the storage model should stay generic.
