# Stat definitions

`stat_definitions` separates what a stat means from the values recorded in games. Definitions belong to a sport and can specify label, category, value type, scope, unit, ordering, and whether lower values are better.

This lets the admin/public UI evolve by configuration as sports are added rather than requiring a new database column for every statistic.
