# Future stat imports

Canonical stat tables should accept data from multiple future ingestion paths: manual admin entry, coach/trusted contributor submission, CSV, official stat exports, and historical research.

Importers should normalize into the same stat-definition model and preserve source/provenance rather than create source-specific public tables.
