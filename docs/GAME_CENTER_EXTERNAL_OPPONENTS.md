# External opponent compatibility

Game Center must remain useful when only one side is a Section X team. The existing games table already supports external opponents. Period scoring and team stats use home/away side, so they can describe both sides without requiring an internal team record.

Athlete stats naturally apply only where Section X athlete records exist.
