// src/components/layout/PublicLayout.tsx
'use client'
import ScoreTicker from './ScoreTicker'
import GlobalSponsorStrip from '@/components/GlobalSponsorStrip'
import ContextualFanBar from '@/components/ContextualFanBar'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { useState } from 'react'
import { Menu, X, Search, Facebook, Instagram } from 'lucide-react'
import { ALL_SPORTS } from '@/lib/constants'

const SECTIONX_FACEBOOK = process.env.NEXT_PUBLIC_SECTIONX_FACEBOOK_URL || 'https://www.facebook.com/profile.php?id=61590744479349'
const SECTIONX_INSTAGRAM = process.env.NEXT_PUBLIC_SECTIONX_INSTAGRAM_URL || 'https://www.instagram.com/sectionxscoreboard/'

export default function PublicLayout({ children }: { children: React.ReactNode }) {
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false)
  const [sportsMenuOpen, setSportsMenuOpen] = useState(false)
  const pathname = usePathname()
  const isHome = pathname === '/'
  const navLinks = [{href:'/',label:'Home'},{href:'/scores',label:'Scores'},{href:'/standings',label:'Standings'},{href:'/schools',label:'Schools'},{href:'/photos',label:'Photos'},{href:'/playoffs',label:'Playoffs'},{href:'/spotlight',label:'Spotlight'}]
  const isActive=(href:string)=>href==='/'?pathname==='/':pathname.startsWith(href)
  const SPRING_SPORTS=[{name:'Baseball',slug:'baseball'},{name:'Softball',slug:'softball'},{name:'Boys Lacrosse',slug:'boys-lacrosse'},{name:'Girls Lacrosse',slug:'girls-lacrosse'},{name:'Boys Golf',slug:'boys-golf'}]
  const FALL_SPORTS=[{name:'Football',slug:'football'},{name:'Boys Soccer',slug:'boys-soccer'},{name:'Girls Soccer',slug:'girls-soccer'},{name:'Volleyball',slug:'volleyball'}]
  const WINTER_SPORTS=[{name:'Boys Basketball',slug:'boys-basketball'},{name:'Girls Basketball',slug:'girls-basketball'},{name:'Boys Hockey',slug:'boys-hockey'},{name:'Girls Hockey',slug:'girls-hockey'}]
  return <div className="min-h-screen flex flex-col">
    <header style={{background:'rgba(5,8,15,0.92)',borderBottom:'1px solid rgba(255,255,255,0.06)',backdropFilter:'blur(20px)'}} className="sticky top-0 z-50"><div className="max-w-7xl mx-auto px-4"><div className="flex items-center justify-between h-14">
      <Link href="/" className="flex items-center gap-2"><div className="flex items-center justify-center w-8 h-8 rounded-lg font-bold text-white text-sm" style={{background:'linear-gradient(135deg,#2563eb,#1d4ed8)',fontFamily:'var(--font-display)',fontWeight:900,boxShadow:'0 0 12px rgba(37,99,235,.4)'}}>SX</div><span className="font-bold text-white hidden sm:block" style={{fontFamily:'var(--font-display)',letterSpacing:'.1em',fontSize:'18px',fontWeight:800}}>SECTION X SCOREBOARD</span><span className="font-semibold text-white sm:hidden" style={{fontFamily:'var(--font-display)',letterSpacing:'.05em',fontSize:'15px'}}>SEC X</span></Link>
      <nav className="hidden md:flex items-center gap-1">{navLinks.map(link=><Link key={link.href} href={link.href} className={`nav-link ${isActive(link.href)?'active':''}`}>{link.label}</Link>)}<div className="relative"><button className={`nav-link flex items-center gap-1 ${pathname.startsWith('/sports')?'active':''}`} onClick={()=>setSportsMenuOpen(!sportsMenuOpen)}>Sports<svg className="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7"/></svg></button>{sportsMenuOpen&&<div className="absolute top-full left-0 mt-1 w-56 rounded-lg shadow-2xl z-50 py-1" style={{background:'var(--bg-elevated)',border:'1px solid var(--border)'}}>{['Spring','Fall','Winter'].map(season=><div key={season}><div className="section-label px-3 py-2">{season}</div>{ALL_SPORTS.filter(s=>s.season===season).map(sport=><Link key={sport.slug} href={`/sports/${sport.slug}`} className="block px-3 py-1.5 text-sm hover:bg-white/5" style={{color:'var(--text-secondary)'}} onClick={()=>setSportsMenuOpen(false)}>{sport.name}</Link>)}</div>)}</div>}</div></nav>
      <div className="flex items-center gap-2"><Link href="/search" aria-label="Search Section X" className="p-2 rounded-lg text-slate-400 hover:text-white hover:bg-white/5"><Search size={19}/></Link><Link href="/submit-score" className="hidden md:flex btn-primary text-xs px-3 py-1.5">+ Submit Score</Link><button className="md:hidden p-2 rounded" style={{color:'var(--text-secondary)'}} onClick={()=>setMobileMenuOpen(!mobileMenuOpen)}>{mobileMenuOpen?<X size={20}/>:<Menu size={20}/>}</button></div>
    </div></div>{mobileMenuOpen&&<div style={{background:'var(--bg-card)',borderTop:'1px solid var(--border)'}}><div className="px-4 py-3 flex flex-col gap-1"><Link href="/search" className="nav-link flex items-center gap-2" onClick={()=>setMobileMenuOpen(false)}><Search size={15}/> Search</Link>{navLinks.map(link=><Link key={link.href} href={link.href} className={`nav-link ${isActive(link.href)?'active':''}`} onClick={()=>setMobileMenuOpen(false)}>{link.label}</Link>)}<div className="section-label mt-3 mb-1">Sports</div>{ALL_SPORTS.map(sport=><Link key={sport.slug} href={`/sports/${sport.slug}`} className="nav-link py-1" onClick={()=>setMobileMenuOpen(false)}>{sport.name}</Link>)}<div className="pt-3 border-t flex flex-col gap-2" style={{borderColor:'var(--border)'}}><Link href="/submit-score" className="btn-primary w-full text-sm py-2" onClick={()=>setMobileMenuOpen(false)}>+ Submit a Score</Link><Link href="/contribute" className="text-center text-xs py-2" style={{color:'var(--text-secondary)'}} onClick={()=>setMobileMenuOpen(false)}>Join the Coverage Team</Link></div></div></div>}</header>
    {!isHome&&<ScoreTicker/>}<GlobalSponsorStrip/><ContextualFanBar/><main className="flex-1">{children}</main>
    <footer style={{background:'var(--bg-card)',borderTop:'1px solid var(--border)'}} className="mt-12"><div className="max-w-7xl mx-auto px-4 py-8"><div className="grid grid-cols-2 md:grid-cols-4 gap-6 mb-8"><div><div className="section-label mb-3">Section X Scoreboard</div><p className="text-xs" style={{color:'var(--text-muted)'}}>The home for Section X high school sports scores, schedules, and standings in Northern New York.</p><div className="mt-4 flex flex-wrap gap-2"><a href={SECTIONX_FACEBOOK} target="_blank" rel="noopener noreferrer" aria-label="Section X Scoreboard on Facebook" className="inline-flex items-center gap-2 rounded-lg border border-white/10 px-3 py-2 text-xs font-bold text-slate-300 hover:text-white hover:bg-white/5"><Facebook size={15}/> Facebook</a><a href={SECTIONX_INSTAGRAM} target="_blank" rel="noopener noreferrer" aria-label="Section X Scoreboard on Instagram" className="inline-flex items-center gap-2 rounded-lg border border-white/10 px-3 py-2 text-xs font-bold text-slate-300 hover:text-white hover:bg-white/5"><Instagram size={15}/> Instagram</a></div></div><div><div className="section-label mb-3">Sports</div><div className="flex flex-col gap-1">{SPRING_SPORTS.map(s=><Link key={s.slug} href={`/sports/${s.slug}`} className="text-xs hover:text-white" style={{color:'var(--text-muted)'}}>{s.name}</Link>)}<div className="mt-2 pt-2" style={{borderTop:'1px solid rgba(255,255,255,.06)'}}><p className="text-xs mb-1" style={{color:'#fbbf2460'}}>FALL 2026</p>{FALL_SPORTS.map(s=><Link key={s.slug} href={`/sports/${s.slug}`} className="block text-xs hover:text-white" style={{color:'var(--text-muted)'}}>{s.name}</Link>)}</div><div className="mt-2 pt-2" style={{borderTop:'1px solid rgba(255,255,255,.06)'}}><p className="text-xs mb-1" style={{color:'#60a5fa60'}}>WINTER 2026-27</p>{WINTER_SPORTS.map(s=><Link key={s.slug} href={`/sports/${s.slug}`} className="block text-xs hover:text-white" style={{color:'var(--text-muted)'}}>{s.name}</Link>)}</div></div></div><div><div className="section-label mb-3">Submit</div><div className="flex flex-col gap-1">{[['/submit-score','Submit a Score'],['/submit-photo','Submit a Photo'],['/contribute','Join the Coverage Team'],['/shoutout','Send a Shoutout'],['/nominate','Nominate an Athlete']].map(([href,label])=><Link key={href} href={href} className="text-xs hover:text-white" style={{color:'var(--text-muted)'}}>{label}</Link>)}</div></div><div><div className="section-label mb-3">Info</div><div className="flex flex-col gap-1"><Link href="/search" className="text-xs hover:text-white" style={{color:'var(--text-muted)'}}>Search</Link><Link href="/advertise" className="text-xs hover:text-white" style={{color:'var(--text-muted)'}}>Advertise / Sponsor</Link><Link href="/schools" className="text-xs hover:text-white" style={{color:'var(--text-muted)'}}>All Schools</Link><Link href="/spotlight" className="text-xs hover:text-white" style={{color:'var(--text-muted)'}}>Spotlight</Link><Link href="/admin" className="text-xs hover:text-white" style={{color:'var(--text-muted)'}}>Admin</Link></div></div></div><div className="pt-6 flex flex-col md:flex-row items-center justify-between gap-2 text-xs" style={{borderTop:'1px solid var(--border)',color:'var(--text-muted)'}}><span>© {new Date().getFullYear()} Section X Scoreboard. All rights reserved.</span><span>Serving St. Lawrence & Franklin County high school sports.</span></div></div></footer>
    <nav className="fixed bottom-0 left-0 right-0 z-50 md:hidden" style={{background:'rgba(5,8,15,.97)',borderTop:'1px solid rgba(255,255,255,.08)',backdropFilter:'blur(20px)',paddingBottom:'env(safe-area-inset-bottom)'}}><div className="flex items-center justify-around px-2 py-2">{[{href:'/',icon:'🏠',label:'Home'},{href:'/scores',icon:'📅',label:'Scores'},{href:'/search',icon:'⌕',label:'Search'},{href:'/schools',icon:'🏫',label:'Schools'},{href:'/submit-score',icon:'✏️',label:'Submit'}].map(item=>{const active=item.href==='/'?pathname==='/':pathname.startsWith(item.href);return <Link key={item.href} href={item.href} className="flex flex-col items-center gap-0.5 px-3 py-1 rounded-lg" style={{color:active?'#60a5fa':'#384d6b',minWidth:'52px'}}><span className="text-xl leading-none">{item.icon}</span><span className="text-xs font-bold uppercase tracking-wider" style={{fontFamily:'var(--font-display)',fontSize:'9px'}}>{item.label}</span></Link>})}</div></nav><div className="h-16 md:hidden"/>
  </div>
}
