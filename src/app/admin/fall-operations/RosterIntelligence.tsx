import { ShieldCheck,Clock3,AlertTriangle } from 'lucide-react'
export default function RosterIntelligence({rows}:{rows:any[]}){const counts=rows.reduce((a:any,r:any)=>{const k=r.freshness_status||'not-scanned';a[k]=(a[k]||0)+1;return a},{}) as Record<string,number>,published=rows.filter(r=>r.arbiter_roster_public).length,stored=rows.reduce((n,r)=>n+Number(r.arbiter_roster_count||0),0),scanned=rows.filter(r=>r.freshness_status).length;return <div className="card p-4"><div className="flex items-center gap-2"><ShieldCheck size={18} className="text-emerald-300"/><h2 className="font-semibold text-white">Roster Publication Intelligence</h2></div><p className="text-xs mt-1" style={{color:'var(--text-muted)'}}>What Arbiter has, what Section X trusts, and what fans are allowed to see.</p><div className="grid grid-cols-2 md:grid-cols-4 gap-2 mt-4">{[[`${published}`,'Verified & public'],[`${counts['prior-season-roster']||0}`,'Prior season held'],[`${counts['awaiting-current-roster']||0}`,'Awaiting roster'],[`${stored}`,'Stored Arbiter rows']].map(([n,l])=><div key={l} className="rounded-lg bg-black/20 p-3 border border-white/[.06]"><b className="text-xl text-white">{n}</b><div className="text-xs mt-1" style={{color:'var(--text-muted)'}}>{l}</div></div>)}</div><div className="mt-4 flex flex-wrap gap-2 text-xs"><span className="inline-flex items-center gap-1 rounded-full border border-emerald-500/20 bg-emerald-500/[.06] px-2.5 py-1 text-emerald-300"><ShieldCheck size={12}/> {published} current verified</span><span className="inline-flex items-center gap-1 rounded-full border border-amber-500/20 bg-amber-500/[.06] px-2.5 py-1 text-amber-300"><Clock3 size={12}/> {scanned-published} scanned & held</span>{(counts['review-needed']||0)>0&&<span className="inline-flex items-center gap-1 rounded-full border border-red-500/20 bg-red-500/[.06] px-2.5 py-1 text-red-300"><AlertTriangle size={12}/> {counts['review-needed']} need review</span>}</div></div>}
